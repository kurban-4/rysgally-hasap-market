name: Release Build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    permissions:
      contents: write
    strategy:
      matrix:
        include:
          - os: macos-latest
            target: aarch64-apple-darwin
            php-binary: php-aarch64-apple-darwin
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            php-binary: php-x86_64-pc-windows-msvc.exe

    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.4'
          extensions: pdo_sqlite, sqlite3, mbstring, zip, fileinfo
          tools: composer:v2

      - name: Install Rust target
        run: rustup target add ${{ matrix.target }}

      - name: Create binaries directory
        shell: bash
        run: mkdir -p src-tauri/binaries

      - name: Build Static PHP (macOS)
        if: matrix.os == 'macos-latest'
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          curl -L https://github.com/crazywhalecc/static-php-cli/releases/latest/download/spc-macos-aarch64.tar.gz -o spc.tar.gz
          tar -xzf spc.tar.gz
          chmod +x spc
          ./spc doctor --auto-fix
          ./spc download --with-php=8.4 --for-extensions="pdo,pdo_sqlite,sqlite3,mbstring,mbregex,fileinfo,zip,openssl,curl,tokenizer,xml,dom,session,ctype,filter" --prefer-pre-built
          ./spc build --build-cli "pdo,pdo_sqlite,sqlite3,mbstring,mbregex,fileinfo,zip,openssl,curl,tokenizer,xml,dom,session,ctype,filter"
          cp buildroot/bin/php src-tauri/binaries/php-aarch64-apple-darwin
          chmod +x src-tauri/binaries/php-aarch64-apple-darwin

      - name: Build Static PHP (Windows)
        if: matrix.os == 'windows-latest'
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          Invoke-WebRequest -Uri "https://github.com/crazywhalecc/static-php-cli/releases/latest/download/spc-windows-x64.exe" -OutFile "spc.exe"
          .\spc.exe doctor --auto-fix
          .\spc.exe download --with-php=8.4 --for-extensions="pdo,pdo_sqlite,sqlite3,mbstring,mbregex,fileinfo,zip,openssl,curl,tokenizer,xml,dom,session,ctype,filter" --prefer-pre-built
          .\spc.exe build --build-cli "pdo,pdo_sqlite,sqlite3,mbstring,mbregex,fileinfo,zip,openssl,curl,tokenizer,xml,dom,session,ctype,filter"
          Copy-Item "buildroot\bin\php.exe" -Destination "src-tauri\binaries\php-x86_64-pc-windows-msvc.exe"

      - name: Install Composer dependencies
        run: |
          cd src-tauri/resources/rysgally-hasap-market
          composer install --no-dev --optimize-autoloader --ignore-platform-reqs

      - name: Create temporary .env
        shell: bash
        run: |
          cd src-tauri/resources/rysgally-hasap-market
          if [ ! -f .env ]; then
            cp .env.example .env || touch .env
          fi

      - name: Fix Pdo\Mysql issue
        shell: bash
        run: |
          CONFIG="src-tauri/resources/rysgally-hasap-market/config/database.php"
          CONNECTOR="src-tauri/resources/rysgally-hasap-market/vendor/laravel/framework/src/Illuminate/Database/Connectors/MySqlConnector.php"
          for FILE in "$CONFIG" "$CONNECTOR"; do
            if [ -f "$FILE" ]; then
              sed -i 's/PDO::MYSQL_ATTR_SSL_CA/1009/g' "$FILE" || sed -i '' 's/PDO::MYSQL_ATTR_SSL_CA/1009/g' "$FILE"
            fi
          done

      - name: Clean Install
        shell: bash
        run: |
          rm -f package-lock.json
          npm install
          cd src-tauri
          cargo fetch

      - name: Build App
        shell: bash
        run: |
          npm run build
          npx tauri build --target ${{ matrix.target }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_SIGNING_PRIVATE_KEY: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY }}
          TAURI_SIGNING_PRIVATE_KEY_PASSWORD: ${{ secrets.TAURI_SIGNING_PRIVATE_KEY_PASSWORD }}

      - name: Debug - Find built files
        if: always()
        shell: bash
        run: find src-tauri/target -name "*.dmg" -o -name "*.exe" -o -name "*.msi" 2>/dev/null || echo "NO FILES FOUND"

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: rysgally-hasap-market-${{ matrix.os }}
          path: |
            src-tauri/target/${{ matrix.target }}/release/bundle/dmg/*.dmg
            src-tauri/target/${{ matrix.target }}/release/bundle/msi/*.msi
            src-tauri/target/${{ matrix.target }}/release/bundle/nsis/*.exe

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        if: startsWith(github.ref, 'refs/tags/')
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          files: |
            src-tauri/target/${{ matrix.target }}/release/bundle/dmg/*.dmg
            src-tauri/target/${{ matrix.target }}/release/bundle/msi/*.msi
            src-tauri/target/${{ matrix.target }}/release/bundle/nsis/*.exe